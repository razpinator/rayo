# Simple CLI Tool
# Demonstrates command line argument handling

import "os"

def main() {
    args = os.Args()
    if len(args) < 2 {
        print("Usage: simple_cli <name>")
        return
    }
    name = args[1]
    print("Hello, " + name + "!")
}
