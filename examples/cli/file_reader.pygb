# File Reader CLI
# Demonstrates file I/O

import "rayo/stdlib/io"

def main() {
    if len(os.Args()) < 2 {
        print("Usage: file_reader <filename>")
        return
    }
    filename = os.Args()[1]
    content = io.ReadFile(filename)
    print("File content:")
    print(content)
}
